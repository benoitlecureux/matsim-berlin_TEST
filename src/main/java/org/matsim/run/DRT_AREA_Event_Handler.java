package org.matsim.run;

import org.geotools.data.FeatureEvent;
import org.geotools.data.FeatureListener;
import org.geotools.data.simple.SimpleFeatureSource;
import org.matsim.api.core.v01.Id;
import org.matsim.api.core.v01.Scenario;
import org.matsim.api.core.v01.network.Link;
import org.matsim.core.utils.gis.ShapeFileReader;

import java.util.Map;

public class DRT_AREA_Event_Handler {
    public static void main(String[] args) {/*
        Scenario scenario = new Scenario;
        Map<Id<Link>, Link> links = scenario.getNetwork().getLinks();
        SimpleFeatureSource shpfile = ShapeFileReader.readDataFile("input/BerlinShapeFile.shp");
        if (links : shpfile.getFeatures().features())

        //FeatureListener featureListener = new Link().getCoord().getX();
        //ShapeFileReader.readDataFile("input/BerlinShapeFile.shp").getSupportedHints().contains(Link);*/
    }
}
